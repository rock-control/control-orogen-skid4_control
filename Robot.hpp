#ifndef ROBOT_HPP
#define ROBOT_HPP

namespace robot
{
    const double WIDTH           = 0.300; // [m]
    const double ROTATION_RADIUS = 0.300; // [m]
    const double WHEEL_RADIUS    = 0.150; // [m]
    const double WHEEL_CIRC      = 2.0 * M_PI * WHEEL_RADIUS; // [m]
}

#endif

